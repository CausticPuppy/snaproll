.build/
.DS_Store
.swiftpm/
*.xcodeproj

# ATV's reference PDFs are copyrighted docs — keep them out of the repo.
*.pdf

# Capture sessions stay local, except the one the regression tests decode
# (protocol reverse-engineering evidence from the VER.2.00 device).
captures/*
!captures/20260701-213259/
